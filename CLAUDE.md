# CLAUDE.md
## 本リポジトリにおける AI エージェント運用方針

本リポジトリは Quarto で構築した個人ウェブサイトであり、GitHub 上で公開され、GitHub Pages として配信される。

プロジェクトの概要・目的は @README.md を参照すること。

---

## 1. AI エージェントの役割と判断基準
- デフォルトは **「進めて報告する」** とする。
- 破壊的操作・方針の根本的変更は、中断して人間に確認を求めること。
- 判断根拠は応答またはコード内コメントで明示すること。

---

## 2. ファイル管理規約
- 作業に必要なファイルは **自律的に作成・編集してよい**。
- ファイル作成時は：
  - 内容が分かる明確なファイル名を用いること
  - プロジェクトのディレクトリ構成に従って配置すること
- `docs/` はビルド出力ディレクトリであり、**直接編集してはならない**。
- 大規模な書き換えを行う場合は、変更理由と影響範囲をコード内コメントで明記すること。

---

## 3. コーディング規約
- **Quarto**：
  - ページは `.qmd` で管理し、直接 `.html` を編集しないこと
  - YAML ヘッダーにタイトルと出力形式を記載すること
  - サイト全体の設定は `_quarto.yml` で管理すること
- **スタイル**：
  - テーマカスタマイズは `custom.scss` を使用すること
  - 追加スタイルは `styles.css` に記述すること
  - インラインスタイル（`style="..."` 属性）は避けること
- 可読性を優先し、巧妙さより分かりやすさを重視すること。

---

## 4. Git / GitHub 運用ルール
- 以下は **人間の指示があった場合のみ** 実行すること：
  - ブランチの作成
  - commit の作成と push
  - PR の作成・merge
  - main ブランチへの直接 push
- **いかなる場合も行ってはならない**：commit 履歴の書き換え

---

## 5. セキュリティ
- API キー・トークン・パスワード等をコードやファイルに含めないこと。
- 個人情報・機微情報を出力しないこと。
- **公開前提のため**、ローカル環境固有のパス・ホスト名・内部 URL 等を含めないこと。

---

## 6. 使用言語
- 応答・コメント・ドキュメントは、原則として **日本語** を使用すること。
- 英語を使用してよい場面：
  - コード本体（識別子・変数名等）
  - ブランチ名・コミットメッセージ
  - 英語の専門用語をそのまま用いる場合

---

## 7. 公開リポジトリとしての前提
- 本リポジトリの内容（コード・コミット履歴・PR・Issue を含む）は **すべて公開される** ことを前提とすること。
- 学生・同業者・第三者が閲覧する可能性を踏まえ、以下に配慮すること：
  - 内輪向けの表現や、文脈なしには理解できない略語を避ける
  - 第三者についての言及（評価・所属の推測等）を含めない
  - 未公開の研究計画・査読中の論文情報・未公表データを含めない
- コミットメッセージや Issue 本文も公開対象であることに留意すること。
